import java.util.*;

public class CesarCrypt {

	public static void main(String [] args)
	{
		
		String sentence;
		String newkey;
		
		Scanner input = new Scanner(System.in);

			
		int [] key = new int [6];
		
		System.out.println("skriv in din nyckel");
			String keyInput = input.nextLine();
			
		for(int i=0; i < keyInput.length(); i++)
		{
			String temp = "" + (char)keyInput.charAt(i);
			int numtemp = Integer.parseInt(temp);
			key[i] = numtemp;
		}
		
		System.out.println("Write the sentence you want to crypt");
		sentence = input.nextLine();
		
		String encrypted = encrypt_message(sentence, key);
		
		System.out.println(encrypted);
		
		String dekrypt = decrypt_message(encrypted, key);
		
		System.out.println(dekrypt);
	}
	
	public static String encrypt_message ( String msg, int[] key)
	{
		String krypt = "";
		int currentKey =0;
		
		for(int i=0;i<msg.length(); i++){
			int chr = msg.charAt(i);
			
			if(chr==32){
				krypt = krypt+(char)chr;
			}else{
				chr = (int)chr+key[currentKey];
				currentKey++;
				if(currentKey == 6){
					currentKey = 0;
				}
				krypt = krypt+(char)chr;
			}
		}
		return krypt;
		
		
	}	
public static String decrypt_message ( String msg, int[] key)
{
	String dekrypt = "";
	int currentKey =0;
	
	for(int i=0;i<msg.length(); i++){
		int chr = msg.charAt(i);
		
		if(chr==32){
			dekrypt = dekrypt+(char)chr;
		}else{
			chr = (int)chr-key[currentKey];
			currentKey++;
			if(currentKey == 6){
				currentKey = 0;
			}
			dekrypt = dekrypt+(char)chr;
		}
	
	
}
	return dekrypt;
} 
}
